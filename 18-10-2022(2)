#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <string>
using namespace std;


int main() {
    int n, k, co, i;
    cin>>n>>k;
    string arr[n];
    for(i=0;i<k;i++){
        if(i >= n){
            cout<<"Houseful";
            return 0;
        }
        cin>>arr[i];
    }
    cout<<"Booked";
    return 0;
}
